import React from 'react';

// Components
import UserList from '../components/UserList/UserList';

const Users = () => {
    return (
        <div>
            <UserList
                users={[]}
            />
        </div>
    );
};

export default Users;
